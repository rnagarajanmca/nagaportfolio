name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

<<<<<<< Updated upstream
=======
    defaults:
      run:
        working-directory: portfolio

    strategy:
      matrix:
        node-version: [20.x]

>>>>>>> Stashed changes
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
<<<<<<< Updated upstream
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: portfolio/package-lock.json

      - name: Install dependencies
        working-directory: ./portfolio
        run: npm ci

      - name: Run linter
        working-directory: ./portfolio
        run: npm run lint

      - name: Run type check
        working-directory: ./portfolio
        run: npx tsc --noEmit

      - name: Run tests
        working-directory: ./portfolio
        run: npm test -- --coverage --watchAll=false

      - name: Build production bundle
        working-directory: ./portfolio
        run: npm run build
        env:
          # Mock environment variables for build
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY || 're_test_key' }}
          NEXT_PUBLIC_BASE_URL: https://nagarajanr.com

      - name: Upload test coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-coverage
          path: portfolio/coverage/
          retention-days: 7
=======
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: portfolio/package-lock.json

      - name: Install dependencies
        working-directory: portfolio
        run: npm install

      - name: Lint
        working-directory: portfolio
        run: npm run lint

      - name: Test
        working-directory: portfolio
        run: npm test -- --runInBand
>>>>>>> Stashed changes
